# ---------------------------------------------------------------------------
# FilmStash Bot - Python dependencies
# Install with:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# Telegram Bot framework (async, v20+). The [webhooks] extra pulls in the
# Tornado-based HTTP server used by run_webhook() in production.
#
# NOTE: 22.x is required on Python 3.13+. Older releases (<= 21.11) call the
# deprecated asyncio.get_event_loop(), which Python 3.14 turned into a hard
# RuntimeError, so the bot crashes on startup with:
#     RuntimeError: There is no current event loop in thread 'MainThread'.
python-telegram-bot[webhooks]==22.8

# Official Supabase client. Wraps PostgREST + GoTrue and gives us the
# `.table("movies").insert(...)` / `.select(...)` fluent API.
supabase==2.9.1

# HTTP client used to talk to the TMDB REST API.
requests==2.32.3

# Loads environment variables from a local .env file into os.environ.
python-dotenv==1.0.1

# ---------------------------------------------------------------------------
# Transitive / helper dependencies (pinned for reproducible installs).
# ---------------------------------------------------------------------------

# Async HTTP layer used by python-telegram-bot. Pinned explicitly so it does
# not drift between environments.
httpx==0.27.2

# REQUIRED for webhook mode (production / Render).
#
# python-telegram-bot treats tornado as an OPTIONAL extra: it is only imported
# when the bot serves webhooks. Long polling never touches it, which is why the
# bot runs fine locally without this line.
#
# Without it, `run_webhook()` crashes at startup with:
#     ModuleNotFoundError: No module named 'tornado'
# (raised from telegram/ext/_utils/webhookhandler.py, which imports tornado.web
# and tornado.httpserver at module level).
#
# It is pinned here so webhook mode works on a clean install, e.g. Render's
# `pip install -r requirements.txt`.
#
# NOTE: the pin MUST stay within the range python-telegram-bot[webhooks] 22.8
# declares for this extra, which is `tornado~=6.5` (i.e. >=6.5, <7.0). Pinning
# an older 6.4.x release makes pip's resolver fail the whole install with:
#     The user requested tornado==6.4.2
#     python-telegram-bot[webhooks] 22.8 depends on tornado~=6.5; extra == "webhooks"
#     ERROR: ResolutionImpossible
# which is exactly what broke the Render build.
tornado==6.5.2
